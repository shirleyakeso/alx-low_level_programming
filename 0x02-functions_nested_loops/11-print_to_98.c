#include <stdio.h>
/**
 *print+to_98-function
 *
 * @n: parameter
 *
 * Return: natural number
 */
void print_to_98(int n)
{
	int upper = 98;
	{
		if (n > upper)
		{
		while (n >= 98)
		{
		if (n != upper)
		{
		printf("%d%s", n, ", ");
		}
		else
		{ 
		printf("%d", n);

		}
		n--;
		}
	}
	else 
	{while (n <= upper)
	{
	if(

	} else if (n > 98)
	while (n > 98)
	{
		print("%d, ", n);
			n--;
	}
	printf("98\n");
}
