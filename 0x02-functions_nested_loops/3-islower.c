#include "main.h"
/**
 * _islower - function
 *
 *@c: the character to evaluate
 *
 * Return: Get 1 if c is uppercase,
 * gey 0 otherwise
 */
int_islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
