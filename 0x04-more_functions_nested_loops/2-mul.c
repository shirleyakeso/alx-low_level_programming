#include "main.h"
#include <stdio.h>

/**
* mul - a function that multiplies two integers.
* @a:First character
* @b:second character to be checked
*
* Return: Always 0
*/

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
